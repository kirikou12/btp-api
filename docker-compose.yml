services:
  db:
    image: postgres:15-alpine
    container_name: btp-db
    environment:
      POSTGRES_DB: btpdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
